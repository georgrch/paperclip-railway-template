#!/usr/bin/env bash
set -euo pipefail

REAL_CODEX="/usr/local/bin/codex"

if [ "${1:-}" = "exec" ]; then
  shift
  exec "$REAL_CODEX" exec --skip-git-repo-check "$@"
else
  exec "$REAL_CODEX" "$@"
fi
